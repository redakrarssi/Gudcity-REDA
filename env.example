# GudCity REDA Environment Configuration Template
# Copy this file to .env and replace placeholder values with your actual credentials

# ===== CRITICAL SECURITY NOTICE =====
# NEVER commit real credentials to version control!
# Replace ALL placeholder values below with your actual configuration

# =============================================================================
# DATABASE CONFIGURATION (REQUIRED)
# =============================================================================
# PostgreSQL database connection string
# Format: postgres://username:password@host:port/database?sslmode=require
DATABASE_URL=postgres://your_db_user:your_db_password@your_host:5432/your_database?sslmode=require
VITE_DATABASE_URL=postgres://your_db_user:your_db_password@your_host:5432/your_database?sslmode=require

# =============================================================================
# JWT AUTHENTICATION SECRETS (REQUIRED)
# =============================================================================
# Generate strong random strings for these (minimum 32 characters)
# Use: openssl rand -base64 32
JWT_SECRET=your_secure_jwt_secret_minimum_32_characters_long
VITE_JWT_SECRET=your_secure_jwt_secret_minimum_32_characters_long
JWT_REFRESH_SECRET=your_secure_jwt_refresh_secret_minimum_32_characters_long
VITE_JWT_REFRESH_SECRET=your_secure_jwt_refresh_secret_minimum_32_characters_long

# =============================================================================
# QR CODE SECURITY (REQUIRED)
# =============================================================================
# Secret key for QR code generation and validation
# Generate with: openssl rand -base64 64
QR_SECRET_KEY=your_secure_qr_secret_key_minimum_64_characters_long
VITE_QR_SECRET_KEY=your_secure_qr_secret_key_minimum_64_characters_long

# QR Code Encryption Key (for protecting sensitive customer data)
# Uses QR_SECRET_KEY if not specified. Generate with: openssl rand -base64 64
QR_ENCRYPTION_KEY=your_secure_qr_encryption_key_minimum_64_characters_long
VITE_QR_ENCRYPTION_KEY=your_secure_qr_encryption_key_minimum_64_characters_long

# QR Code Encryption Settings
QR_ENCRYPTION_ENABLED=true
VITE_QR_ENCRYPTION_ENABLED=true

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================
# Application environment
VITE_APP_ENV=development
NODE_ENV=development

# API Configuration
VITE_API_URL=http://localhost:3000
APP_URL=http://localhost:5173

# =============================================================================
# SECURITY CONFIGURATION
# =============================================================================
# CORS Origins (comma-separated for production)
VITE_CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Content Security Policy
VITE_CSP_ENABLED=true

# Debug mode (set to false in production)
VITE_DEBUG=true

# Rate limiting
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW_MS=900000

# =============================================================================
# OPTIONAL SERVICES
# =============================================================================
# Mapbox (for location features)
VITE_MAPBOX_ACCESS_TOKEN=your_mapbox_token_here

# Email service (if using)
SMTP_HOST=your_smtp_host
SMTP_PORT=587
SMTP_USER=your_smtp_user
SMTP_PASS=your_smtp_password

# =============================================================================
# TEST ENVIRONMENT (DEVELOPMENT ONLY)
# =============================================================================
# Password for test users (development/testing only)
TEST_USER_PASSWORD=your_secure_test_password_here

# =============================================================================
# FEATURE FLAGS
# =============================================================================
VITE_ENABLE_FEEDBACK=true
VITE_ENABLE_ANIMATIONS=true
VITE_ENABLE_ANALYTICS=true
VITE_SHOW_MOCK_NOTICE=false
VITE_MOCK_AUTH=false
VITE_MOCK_DATA=false

# =============================================================================
# PRODUCTION DEPLOYMENT
# =============================================================================
# Frontend URL (for production)
FRONTEND_URL=https://your-production-domain.com

# SSL Configuration
SSL_ENABLED=true

# =============================================================================
# SECURITY CHECKLIST
# =============================================================================
# Before deploying to production:
# [ ] Replace ALL placeholder values with real credentials
# [ ] Generate strong random secrets using secure methods
# [ ] Set VITE_DEBUG=false for production
# [ ] Configure proper CORS origins
# [ ] Enable SSL with proper certificates
# [ ] Review and test all environment variables
# [ ] Ensure .env file is in .gitignore
# [ ] Never commit real credentials to version control
